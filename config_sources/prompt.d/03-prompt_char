#!/usr/bin/env bash

function __prompt_char () {
  unadorned_result__="$"
  result__="${yellow__}${unadorned_result__}${no_color__}"
  prompt_color__="${no_color__}"
  if [ `whoami` = "root" ]
  then
    unadorned_result__="#"
    result__="${red__}${unadorned_result__}${no_color__}"
    prompt_color__="${light_red__}"
  fi
}
